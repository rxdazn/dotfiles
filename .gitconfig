[core]
    editor = vim
    filemode = false
[user]
    name = rxdazn 
    email = rxdazn@gmail.com
[alias]
    st = status
    co = checkout
    tree = log --graph --all --abbrev-commit --date=short --pretty='%C(auto)%h%d %C(reset)%s %C(bold blue)(%ce) %C(reset)%C(green)(%cd)'
[color]
    ui = true
